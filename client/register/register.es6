angular
  .module('bookShowcase.register', ['bookShowcase.common'])
  .controller('RegisterCtrl', ['$log', 'bkAuth', ($log, bkAuth) => {
    var registerCtrl = this;
    registerCtrl.registerData = {};
    registerCtrl.attemptRegister = () => {
      
    }
  }])